---
export async function getStaticPaths() {
  return [
    { params: { slug: 1 } },
    { params: { slug: 2 } },
    { params: { slug: 3 } }
  ];
}

const { slug } = Astro.params;
---

<h1>{slug}</h1>